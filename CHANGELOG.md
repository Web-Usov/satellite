# История изменений

## v1.3.1 - 18 января 2025

### 📦 Обновление пакетов и лицензирование

#### Изменения в именовании
- **Scoped packages**: переход на `@satellite/*` namespace
  - `@satellite/monorepo` - корневой пакет
  - `@satellite/server` - серверный пакет  
  - `@satellite/client` - клиентский пакет

#### Лицензирование
- **MIT License** - добавлена открытая лицензия
- **Авторство**: Alex Usov <web.usov@ya.ru>
- **Файлы**: LICENSE, обновлены все package.json

#### GitHub интеграция
- **GitHub Actions** - автоматические релизы
- **Release workflow** - создание архивов при push тегов
- **Скрипт релизов** - `create-release.sh` для удобства

#### Документация
- **README.md** - добавлен раздел "Релизы"
- **Инструкции** - пошаговое создание релизов
- **Ссылки** - на GitHub Releases

---

### 🎉 Режим Input TLE

#### Новая функциональность
- **Ручной ввод TLE данных** для оффлайн расчетов
- **Полностью локальные расчеты** без API запросов
- **Валидация TLE** формата в реальном времени
- **Сохранение TLE** в localStorage

#### Компоненты
- `TLEInput` - управление пользовательскими TLE
  - Добавление/редактирование TLE
  - Продвинутая валидация формата:
    - Регулярное выражение для проверки структуры
    - Проверка длины (69 символов)
    - **Проверка контрольных сумм** (checksum)
    - Проверка совпадения номера спутника
    - Поддержка опционального заголовка (max 24 символа)
  - Список сохраненных TLE с возможностью редактирования

#### Store расширения
- `userTLEs: UserTLE[]` - хранилище TLE данных
- `addUserTLE()` - добавить TLE
- `updateUserTLE()` - обновить TLE
- `removeUserTLE()` - удалить TLE
- `clearUserTLEs()` - очистить все TLE

#### API расширения
- `getAllPassesWithInputTle()` - расчет проходов по пользовательским TLE
- Обновлен `getAllPasses()` для поддержки 3 режимов:
  - `api-tle` - TLE через API + локальные расчеты
  - `api-radio` - прямой запрос к N2YO API
  - `input-tle` - ручной ввод + локальные расчеты ✨

#### UI изменения
- Условный рендеринг: `TLEInput` ↔️ `SatelliteInput`
- Автоматическое переключение при смене режима
- Обновлена информация о количестве расчетов

#### Типы
```typescript
interface UserTLE {
  id: string;
  name: string;
  line1: string;
  line2: string;
  createdAt: number;
}
```

### 📋 Преимущества Input TLE режима
- ✅ Нет сетевых запросов
- ✅ Нет ограничений API
- ✅ Работает оффлайн
- ✅ Полный контроль над данными
- ✅ Возможность использовать устаревшие TLE
- ✅ Мгновенные расчеты

### 📤 Экспорт данных (v1.3.0)

#### Новая функциональность
- **Экспорт результатов в CSV** файл
- **Расширяемая архитектура** для добавления новых форматов
- **Два режима экспорта**:
  - Экспорт всех станций в один файл
  - Экспорт отдельной станции

#### Архитектура экспорта
```typescript
services/export/
├── types.ts              // Общие типы и интерфейсы
├── exporters/
│   ├── BaseExporter.ts   // Базовый класс для всех экспортеров
│   └── CsvExporter.ts    // Реализация CSV экспорта
├── ExportManager.ts      // Менеджер управления экспортерами
└── index.ts              // Реэкспорт
```

#### Реализованные форматы
- ✅ **CSV** - текстовый формат с разделителями

#### Готово к добавлению
- 🔜 **JSON** - структурированные данные
- 🔜 **XLSX** - Excel файлы
- 🔜 **iCal** - календарные события

#### CSV формат
Включает следующие поля:
- Station (если экспорт нескольких станций)
- Satellite
- Start Time (UTC)
- Max Elevation Time (UTC)
- End Time (UTC)
- Max Elevation (°)
- Start Azimuth (°)
- Start Direction
- End Azimuth (°)
- End Direction
- Duration (min)

#### UI
- **Кнопка "Экспорт CSV"** в заголовке расписания (экспорт всех станций)
- **Иконка экспорта** в каждом аккордеоне станции (экспорт одной станции)
- Автоматическое именование файлов с временной меткой
- Индикация процесса экспорта

---

## v1.2.0 - 14 октября 2025

### 🎯 Улучшенная точность расчетов

#### Оптимизация алгоритма
- **Уменьшен шаг расчета**: 5 секунд (было 10 секунд)
- **Бинарный поиск**: уточнение времени пересечения горизонта
  - 20 итераций
  - Точность 0.1 секунды
- **Двухэтапный поиск максимума**:
  - Грубый поиск: 50 измерений по всему проходу
  - Точный поиск: 60 измерений в узком окне
- **Логика прохода**: от горизонта (0°) до горизонта
  - Фильтрация по `minElevation` после расчета
  - Корректная длительность проходов

#### Результат
✅ Точность < 1% отклонения от эталонного N2YO API

### 🕐 UTC форматирование

- Все даты и время отображаются в UTC
- Добавлены метки "(UTC)" в заголовках столбцов
- Форматирование через `dayjs().utc()`

### 🔧 Улучшение конфигурации

#### Убрали Vite proxy
- Прямое обращение к бэкенду через `VITE_BACKEND_API_URL`
- Единое решение для development и production

#### Файлы конфигурации
- `apps/client/.env`:
  ```env
  VITE_BACKEND_API_URL=http://localhost:3001/api/satellite
  ```
- `apps/server/.env`:
  ```env
  N2YO_API_KEY=your_api_key
  ```

### 🎨 UI улучшения

- **ModeSelector перемещен** из header в левый сайдбар
- **Горизонтальный layout** для переключателя режимов
- **Упрощена таблица**: убран столбец "Азимут макс."

### 📦 API изменения

#### Удалены поля из PassData
- `maxAz` (число)
- `maxAzCompass` (строка)

#### Текущая структура PassData
```typescript
interface PassData {
  startUTC: number;
  maxUTC: number;
  endUTC: number;
  maxEl: number;
  startAz: number;
  endAz: number;
  startAzCompass: string;
  endAzCompass: string;
  satelliteName?: string;
  stationId?: string;
  stationName?: string;
}
```

### 📚 Документация

#### Удалены файлы
- ❌ `TLE_USAGE.md`
- ❌ `CHANGELOG_V1.1.md`
- ❌ `MIGRATION_V1.1.md`

#### Обновлены файлы
- ✅ `README.md`
  - Обновлена конфигурация переменных окружения
  - Добавлено описание высокоточного алгоритма
  - Обновлено расположение ModeSelector
  - Добавлена информация о UTC форматировании
- ✅ `PROJECT_ANALYSIS.md`
  - Версия обновлена до v1.2.0
  - Добавлены новые функции
  - История изменений
- ✅ `AGENTS.MD`
  - Обновлена структура PassData
  - Обновлено описание CORS решения

---

## v1.1.0 - 13 октября 2025

### Локальные расчеты орбит
- Интеграция satellite.js v6.0.1
- Реализация SGP4/SDP4 алгоритмов
- TLE API интеграция
- Кэширование TLE данных

### Режимы расчета
- API TLE (локальные расчеты)
- API Radio (N2YO API)
- Input TLE (готовится к релизу)

---

## v1.0.0 - 11 октября 2025

### Базовая функциональность
- React + TypeScript + Material-UI
- Управление спутниками и станциями
- Расчет проходов через N2YO API
- Docker контейнеризация
- Responsive дизайн

